import 'package:flutter/material.dart';

class WomenManifestoScreen extends StatelessWidget {
  const WomenManifestoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('আহ্বান — মহিলা মজলিস')),
      body: const Center(child: Text('মহিলা মজলিস আহ্বান')),
    );
  }
}
