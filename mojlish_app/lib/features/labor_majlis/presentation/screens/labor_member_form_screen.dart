import 'package:flutter/material.dart';

class LaborMemberFormScreen extends StatelessWidget {
  const LaborMemberFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('প্রাথমিক সদস্য ফরম — শ্রমিক মজলিস')),
      body: const Center(child: Text('শ্রমিক মজলিস সদস্য ফরম')),
    );
  }
}
